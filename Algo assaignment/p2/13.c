/////not understand
//copy by Chat GPT
//copy by Chat GPT
//copy by Chat GPT
//copy by Chat GPT
//copy by Chat GPT
//copy by Chat GPT



#include <stdio.h>
#include <math.h>

int main() {
    int x;
    double y;

    printf(" x  |   y\n");
    printf("------------\n");

    for (x = 1; x <= 180; x++) {
        y = sin(x) + cos(x);
        printf("%3d | %.6lf\n", x, y);
    }

    return 0;
